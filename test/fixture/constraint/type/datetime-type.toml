[schema]
type = "datetime"

    [tests.valid]
    value = 1979-05-27T07:32:00Z
    expected = []

    [tests.invalid]
    value = "foo"
    expected = [
        "Validation failed for value at document root: The value must be of type 'datetime'."
    ]
