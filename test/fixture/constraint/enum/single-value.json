{
    "schema": {
        "enum": ["foo"]
    },

    "tests": {
        "valid": {
            "value": "foo",
            "expected": []
        },

        "invalid": {
            "value": "bar",
            "expected": [
                "Validation failed for value at document root: The value must be equal to \"foo\"."
            ]
        }
    }
}
